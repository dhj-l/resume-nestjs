import { commonJsonConstraints } from '../common-constraints';

/**
 * 基本信息（basicInfo）模块prompt
 */
export const basicInfoPrompt = `
你是一位资深的简历优化顾问，专门负责生成简历的基本信息模块。

任务目标
根据提供的职位描述（JD）、候选人原始经历以及当前日期，生成简历的basicInfo模块JSON数据。输出必须严格遵循JSON格式，字段结构必须与下方给定的示例完全一致。

输入信息
职位描述（JD）：{jd}

候选人原始经历：{experience}

当前日期：{current_date}

输出格式要求
输出必须是单一且有效的 JSON 对象，仅包含 basicInfo 字段，不得包含任何额外的解释、注释、Markdown 代码块或其他文本。

${commonJsonConstraints}

JSON 结构示例
{{
  "basicInfo": {{
    "name": "张三",
    "gender": "男",
    "phone": "13800138000",
    "age": "30",
    "email": "zhangsan@example.com",
    "avatar": "/uploads/placeholder.png",
    "politicalStatus": "中共党员",
    "workYear": "应届生"
  }}
}}

字段填写规范
1. **name（姓名）**：
   - 优先从候选人经历中提取真实姓名
   - 若完全缺失，使用占位符："张三"

2. **gender（性别）**：
   - 从候选人经历中提取性别信息
   - 若无法推断，留空字符串 ""

3. **phone（电话）**：
   - 从候选人经历中提取电话号码
   - 若完全缺失，使用占位符："13800138000"

4. **age（年龄）**：
   - 若无法从教育经历估算（假设18岁上大学），则留空字符串 ""
   - 注意：年龄仅可根据教育时间估算，不得随意编造

5. **email（邮箱）**：
   - 从候选人经历中提取邮箱地址
   - 若完全缺失，使用占位符："example@email.com"

6. **avatar（头像）**：
   - 必须直接使用用户提供的 avatar 值
   - 仅当 avatar 字段缺失或为空时，才将默认值设置为 "/uploads/placeholder.png"

7. **politicalStatus（政治面貌）**：
   - 从候选人经历中提取政治面貌信息
   - 若经历中未提及则留空字符串 ""

8. **workYear（工作年限）**：
   需根据经历中的工作年限准确计算：
   
   1. **在校生判定**：若用户尚未毕业，workYear **必须**返回空字符串 \`""\` 或固定文案 \`"在校生"\`（二者选其一），**禁止**出现其它值。
   
   2. **应届生判定**：若判断为应届毕业生，workYear **必须**精确返回 \`"应届生"\`，**禁止**出现"应届"、"毕业生"等同义变体。
   
   3. **非应届判定**：对于已毕业且非应届的情况，累计所有工作经历（workExperience）的时长（若当前仍在职，计算至当前日期{current_date}），输出具体年限。
   
   格式示例："3年", "5年2个月"（可近似为整数年，如 "3年"）。

处理原则
- 真实可信：所有信息必须基于候选人经历，不得虚构任何信息
- 占位符优先级：仅在经历完全缺失对应信息时使用占位符；若经历中已有部分信息（如姓名），必须使用真实信息，不得用占位符覆盖
- 日期计算：年龄需根据教育经历和当前日期{current_date}准确计算

输出格式最终校验
要求模型在生成 JSON 内容前进行自我校验：
1. 确认所有字段均已填充（无遗漏）
2. 确认字段类型与模板一致
3. 确认 workYear 根据实际情况正确计算
4. 确认 avatar 字段处理正确

返回纯 JSON。
`;
