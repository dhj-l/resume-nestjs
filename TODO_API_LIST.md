# 代做接口清单 (TODO API List)

> 更新日期：2026-02-21
> 依据：`API_DOCS.md` v1.0 及当前代码库分析

## 1. 用户模块 (User Module)

| 接口名称     | HTTP 方法 | 路由路径              | 功能描述                                           | 完成状态  | 预估工时 | 责任人 |
| :----------- | :-------- | :-------------------- | :------------------------------------------------- | :-------- | :------- | :----- |
| **退出登录** | POST      | `/api/v1/user/logout` | 清除用户登录状态（服务端黑名单或客户端清理 Token） | 🔴 待开发 | 2h       | TBD    |

### 1.1 退出登录

- **功能描述**: 用户主动退出，使当前 Token 失效。虽然 JWT 是无状态的，但为了安全（如用户在公共设备登录），通常需要实现 Token 黑名单机制（Redis）或仅在客户端清除（若无服务端状态）。鉴于文档提及，建议实现服务端黑名单或预留接口。
- **请求结构**:
  - Header: `Authorization: Bearer {token}`
- **响应结构**:
  - Status: 200
  - Body: `{ code: 200, message: "退出成功", data: null }`
- **验收标准**:
  1.  调用接口后，返回成功消息。
  2.  (可选/进阶) 调用该接口后，使用原 Token 访问受保护接口应返回 401 Unauthorized。

---

## 2. 简历模块 (Resume Module)

| 接口名称          | HTTP 方法 | 路由路径                     | 功能描述                              | 完成状态  | 预估工时 | 责任人 |
| :---------------- | :-------- | :--------------------------- | :------------------------------------ | :-------- | :------- | :----- |
| **简历预览**      | GET       | `/api/v1/resume/:id/preview` | 获取简历的渲染后预览数据（HTML/图片） | 🔴 待开发 | 4h       | TBD    |
| **搜索简历**      | GET       | `/api/v1/resume/search`      | 按关键词搜索当前用户的简历            | 🔴 待开发 | 3h       | TBD    |
| **导出简历 JSON** | GET       | `/api/v1/resume/:id/export`  | 导出简历原始数据为 JSON 文件          | 🔴 待开发 | 2h       | TBD    |
| **导入简历 JSON** | POST      | `/api/v1/resume/import`      | 上传 JSON 文件并创建新简历            | 🔴 待开发 | 3h       | TBD    |

### 2.1 简历预览

- **功能描述**: 获取简历的预览内容。目前 `download` 接口生成 PDF，此接口可能用于前端轻量级预览（或生成缩略图）。
- **请求结构**: `GET /api/v1/resume/:id/preview`
- **响应结构**: `{ code: 200, data: { html: "...", css: "..." } }` 或 图片 URL。
- **验收标准**: 返回可供前端渲染的完整 HTML/CSS 或预览图 URL。

### 2.2 搜索简历

- **功能描述**: 在用户拥有的简历中，根据标题、技能、职位等关键词进行模糊搜索。
- **请求结构**: `GET /api/v1/resume/search?q=keyword`
- **响应结构**: 返回简历列表数组。
- **验收标准**: 能够正确筛选出包含关键词的简历。

### 2.3 导出简历 JSON

- **功能描述**: 将数据库中的简历结构化数据导出为 JSON 文件，便于备份或迁移。
- **请求结构**: `GET /api/v1/resume/:id/export`
- **响应结构**: 文件流 (`application/json`)。
- **验收标准**: 下载的文件内容与数据库存储结构一致。

### 2.4 导入简历 JSON

- **功能描述**: 将导出的 JSON 简历数据重新导入，创建一份新简历。
- **请求结构**: `POST /api/v1/resume/import` (Body: JSON Content or File)
- **响应结构**: 返回新创建的简历对象。
- **验收标准**: 导入后生成的简历数据完整，且归属于当前用户。

---

## 3. 模板模块 (Template Module)

| 接口名称             | HTTP 方法 | 路由路径                              | 功能描述                                | 完成状态  | 预估工时 | 责任人 |
| :------------------- | :-------- | :------------------------------------ | :-------------------------------------- | :-------- | :------- | :----- |
| **使用模板创建简历** | POST      | `/api/v1/template/:id/use`            | 基于指定模板快速创建一份新简历          | 🔴 待开发 | 2h       | TBD    |
| **按分类获取模板**   | GET       | `/api/v1/template/category/:category` | 获取指定分类下的所有模板                | 🔴 待开发 | 2h       | TBD    |
| **获取热门模板**     | GET       | `/api/v1/template/popular`            | 根据使用次数 (`usedCount`) 降序获取模板 | 🔴 待开发 | 2h       | TBD    |
| **收藏模板**         | POST      | `/api/v1/template/:id/favorite`       | 用户收藏指定模板                        | 🔴 待开发 | 3h       | TBD    |
| **取消收藏模板**     | DELETE    | `/api/v1/template/:id/favorite`       | 取消对指定模板的收藏                    | 🔴 待开发 | 2h       | TBD    |
| **获取收藏模板**     | GET       | `/api/v1/template/favorites`          | 获取当前用户收藏的所有模板              | 🔴 待开发 | 2h       | TBD    |

### 3.1 使用模板创建简历

- **功能描述**: 这是一个快捷接口，逻辑可能类似 `POST /resume` (带 `templateId`)，但语义更明确，且可能包含 `usedCount` 自增逻辑。
- **验收标准**: 创建成功，且对应模板的 `usedCount` +1。

### 3.2 按分类/热门获取模板

- **功能描述**: 丰富模板查询维度。
- **验收标准**: 返回结果符合分类或排序要求。

### 3.3 收藏功能 (Favorite)

- **功能描述**: 社交化功能，需要新增 `UserTemplateFavorite` 关联表或在 User/Template 表中存储关联 ID。
- **数据模型变更**: 需新增数据模型支持收藏关系。
- **验收标准**: 收藏后能在“我的收藏”列表中看到；取消后不再显示。

---

## 4. 缺失的功能模块 (Missing Modules)

| 模块名称         | 功能描述          | 涉及接口                               | 优先级 | 预估工时 |
| :--------------- | :---------------- | :------------------------------------- | :----- | :------- |
| **Share 模块**   | 简历分享功能      | 生成分享链接、验证分享码、公开访问简历 | 高     | 8h       |
| **History 模块** | 历史记录/版本控制 | 保存简历快照、回滚版本、查看历史记录   | 中     | 12h      |

### 4.1 Share 模块

- **核心逻辑**: 生成唯一的分享 Token/URL，设置有效期和访问密码（可选）。
- **接口**: `POST /share/create`, `GET /share/:token`。
- **权限**: 公开访问接口需跳过 JWT 校验。

### 4.2 History 模块

- **核心逻辑**: 每次重大修改保存一个版本快照。
- **接口**: `GET /resume/:id/history`, `POST /resume/:id/rollback/:versionId`。

---

## 5. 待优化/修复项 (Refactoring)

- **权限控制**: 目前部分接口（如上传）可能未严格添加 `@UseGuards(JwtAuthGuard)`，需全面排查。
- **异常处理**: 统一异常过滤器 (`AllExceptionsFilter`) 需确保覆盖所有业务异常，返回标准 JSON 格式。
- **环境变量**: 上传接口中的 URL `http://localhost:3000/...` 为硬编码，需替换为 ConfigService 配置。
