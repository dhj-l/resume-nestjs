---
paths: "docs/**"
description: "项目 API 文档和技术文档，Markdown 格式"
---

# 模块：docs

## 职责
存放项目的技术文档和 API 接口说明文档，供开发者和外部调用方参考。

## 文件列表
| 文件 | 职责 |
|------|------|
| `api/sse-resume-generation.md` | SSE 简历生成接口文档（请求参数、响应格式、11 个生成模块说明） |

## 依赖关系
- **上游**：无
- **下游**：无
- **外部**：无

## 常见修改点
- API 变更时同步更新 `docs/api/` 下对应文档
- 新增接口时在 `docs/api/` 下创建对应 `.md` 文件
